Проанализировать структуру БД vk, которую мы создали на занятии, и внести предложения по
усовершенствованию (если такие идеи есть). Напишите пожалуйста, всё-ли понятно по
структуре.

1)

Пользователь может удалить свой профиль, поэтому необходим атрибут is_active, который будет равен 1 в случае,
если пользователь активен и 0 в том случае, когда пользователь удалил свою страницу.

2)
Для атрибутов: 
  status VARCHAR(30) COMMENT "Текущий статус",
  city VARCHAR(130) COMMENT "Город проживания",
  country VARCHAR(130) COMMENT "Страна проживания"
 
из таблицы profiles (Таблицы профилей) необходимо создать id и организовать отдельные справочники,
чтобы хранить числовые эквиваленты вместо символьных, что позволит оптимизировать расход использования 
дискового пространства т.к. числовые типы занимают меньший объем памяти по сравнению с символьными (особенно 
для национальных алфавитов).

3) 
Заменить расшифровки SERIAL на сам SERIAL (SERIAL =  BIGINT UNSIGNED NOT NULL AUTO_INCREMENT UNIQUE)


Добавить необходимую таблицу/таблицы для того, чтобы можно было использовать лайки
для медиафайлов, постов и пользователей.

Мне кажется достаточно создать одну таблицу 

CREATE TABLE  like_stats (
    id SERIAL PRIMARY KEY COMMENT "Идентификатор строки",
    user_id INT UNSIGNED NOT NULL COMMENT "Ссылка на пользователя, который загрузил файл",
    filename VARCHAR(255) NOT NULL COMMENT "Путь к файлу",
    media_type_id INT UNSIGNED NOT NULL COMMENT "Ссылка на тип контента",
    cnt_likes INT COMMENT "Количество лайков"
);

и добавить в таблицу media атрибут photo_id с ссылкой на фотографию пользователя для возможности оценивать лайками точно 
так же как и посты и медиафайлы.